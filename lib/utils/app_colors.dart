import 'package:flutter/material.dart';

class AppColors {
  // Cores do tema claro
  static const Color primary = Color(0xFF9867EC);
  static const Color secondary = Color(0xFFFACF5A);
  static const Color background = Color(0xFFF3F3F3);
  static const Color text = Color(0xFF222222);

  // Cores do tema escuro
  static const Color darkPrimary = Color(0xFF7B52C1);
  static const Color darkBackground = Color.fromRGBO(27, 28, 29, 0);
  static const Color darkText = Color(0xFFFFFFFF);

  // Cores adicionais (comuns)
  static const Color error = Colors.red;
  static const Color success = Colors.green;
}
